<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permissão de Internet para as chamadas Retrofit -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.QuestGen"
        android:usesCleartextTraffic="true"
        tools:targetApi="28">

        <!-- ══ PONTO DE ENTRADA DO APP ══ -->
        <activity
            android:name=".LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Cadastro -->
        <activity
            android:name=".CadastroActivity"
            android:exported="false" />

        <!-- Seleção de Jogo -->
        <activity
            android:name=".GameSelectionActivity"
            android:exported="false" />

        <!-- Detalhes do Desafio -->
        <activity
            android:name=".ChallengeDetailActivity"
            android:exported="false" />

        <!-- Home (tela principal pós-login) -->
        <activity
            android:name=".HomeActivity"
            android:exported="false" />

        <!-- MainActivity — mantida como fallback de navegação -->
        <activity
            android:name=".MainActivity"
            android:exported="false" />

    </application>

</manifest>